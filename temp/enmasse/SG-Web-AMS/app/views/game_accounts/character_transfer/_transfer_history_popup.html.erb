<div class="transfer-popup" id="transfer-history-popup" style="display: none;">
  <div class="heading">
    <h2><%= tslt('transfer a character') %></h2>
  </div>
  <div class="transfer-description">
    <span class="date white"><%= Time.now.strftime("%b %d, %Y") %></span>
    <%= tslt('transferring') %>
    <span class="character-name white"><%= @character_transfer.character_name %></span>

    <% if @character_transfer.to_server_id.present? %>
      <%= tslt('from') %>
      <span class="from-server white"><%= @character_transfer.from_server_name %></span>
      <%= tslt('to') %>
      <span class="to-server white"><%= @character_transfer.to_server_name %></span>
    <% end %>
  </div>

  <%= render :partial => "game_accounts/character_transfer/statuses/#{ CharacterTransfer::STATUS[@character_transfer.status] }_popup",
             :locals => { :character_transfer => @character_transfer } %>
</div>
