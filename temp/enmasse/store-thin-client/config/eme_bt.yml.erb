eme_bt:
  url: <%= "#{fetch(:eme_bt_url)}" %>
  ssl: <%= "#{fetch(:eme_bt_ssl)}" %>
  name: <%= "#{fetch(:eme_bt_name)}" %>
  use_memcache: <%= "#{fetch(:eme_bt_use_memcache)}" %>
  type: <%= "#{fetch(:eme_bt_type)}" %>
  log_level: <%= "#{fetch(:eme_bt_log_level)}" %>
  resource: <%= "#{fetch(:eme_bt_resource)}" %>
  game: <%= "#{fetch(:eme_bt_game)}" %>
