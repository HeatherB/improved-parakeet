pay_letter:
  url: <%= "#{fetch(:payletter_url)}" %>
  purchase_url: <%= "#{fetch(:payletter_purchase_url)}" %>
  ssl: <%= "#{fetch(:payletter_ssl)}" %>
  name: <%= "#{fetch(:payletter_name)}" %>
  use_memcache: <%= "#{fetch(:payletter_use_memcache)}" %>
  cache_prefix: <%= "#{fetch(:payletter_cache_prefix)}" %>
  memcache_hosts: 
  <% fetch(:payletter_memcached_hosts).each do |memsrv| %>
    - <%= memsrv %>
  <% end %>
  type: <%= "#{fetch(:payletter_type)}" %>
  api_key: 
  mystery_key: 
