<%= "#{fetch(:rails_env)}" %>:
  servers:
  <% fetch(:memcached_servers).each do |memsrv| %>
  - <%= "#{memsrv}:#{fetch(:memcached_port)}" %>
  <% end %>
  options:
    namespace: <%= "#{fetch(:memcached_name)}" %>
  